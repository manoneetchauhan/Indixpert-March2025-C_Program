#include <stdio.h>
int main()
{
    int number;

    printf("Please Enter Any Number: ");

    scanf("%d", &number);

    if (number < 0, (number > 0))
    {
        printf("Positive Number: ");
    }

    else
    {
        printf("Negative Number: ");
    }
    return 0;
}